spring:
  application:
    name: ms-naissances
  jpa:
    hibernate:
      ddl-auto: update
    #database-platform: org.hibernate.dialect.MySQLDialect

  server:
    servlet:
      context-path: /api

spring:
  application:
    name: ms-naissances
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect
  jackson:
    default-property-inclusion: non_null
    serialization:
      INDENT_OUTPUT: true
server:
  servlet:
    context-path: /api

